# Public site origin used for canonical URLs, Open Graph metadata, sitemap.xml, and robots.txt.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only credentials used by /manus-storage/[...key] to obtain signed asset URLs.
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
